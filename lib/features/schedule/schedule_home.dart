import 'package:flutter/material.dart';

// need to view the schedule
// need to enter the reps, weight etc in the schedule
// need to mark the attendance - if has
//  performance viewer - weight, height, reps etc
class Viewschedule extends StatefulWidget {
  const Viewschedule({super.key});

  @override
  State<Viewschedule> createState() => _ViewscheduleState();
}

class _ViewscheduleState extends State<Viewschedule> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
